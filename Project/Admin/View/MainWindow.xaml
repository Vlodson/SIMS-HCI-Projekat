<Window x:Class="Admin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Admin"
        mc:Ignorable="d"
        FontSize="20"
        Title="MainWindow" Height="750" Width="430">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="10" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>

        <Button x:Name="addRoomBtn" Grid.Column="1" Grid.Row="1" Margin="0,0,0,10" Content="Add Room" Click="addRoomBtn_Click" />
        <Button x:Name="editRoomBtn" Grid.Column="1" Grid.Row="2" Margin="0,0,0,10" Content="Edit Room" Click="editRoomBtn_Click" />
        <Button x:Name="removeRoomBtn" Grid.Column="1" Grid.Row="3" Margin="0,0,0,10" Content="Remove Room" Click="removeRoomBtn_Click"  />
    </Grid>
</Window>
