<UserControl x:Class="Admin.Views.EquipmentTransferTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Admin.Views"
             xmlns:vm="clr-namespace:Admin.ViewModel"
             xmlns:vml="clr-namespace:Admin.VML"
             vml:ViewModelLocator.AutoHookedUpViewModel="True"
             mc:Ignorable="d" 
             FontSize="14"
             d:DesignHeight="750" d:DesignWidth="430">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:FriendlyEquipmentTransfer}" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <TextBox Text="{Binding Path=OriginRoom, Mode=TwoWay}" IsReadOnly="True" Grid.Column="0" />
                <TextBox Text="{Binding Path=DestinationRoom, Mode=TwoWay}" IsReadOnly="True" Grid.Column="1" />
                <TextBox Text="{Binding Path=Equipment, Mode=TwoWay}" IsReadOnly="True" Grid.Column="2" />
                <TextBox Text="{Binding Path=EndDate, Mode=TwoWay}" IsReadOnly="True" Grid.Column="3" />
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Origin" Grid.Column="0" Grid.Row="0" FontWeight="Bold" />
        <TextBlock Text="Dest" Grid.Column="1" Grid.Row="0" FontWeight="Bold" />
        <TextBlock Text="Equipment" Grid.Column="2" Grid.Row="0" FontWeight="Bold" />
        <TextBlock Text="End Date" Grid.Column="3" Grid.Row="0" FontWeight="Bold" />

        <ListBox ItemsSource="{Binding EquipmentTransfers}" SelectedItem="{Binding SelectedEquipmentTransfer}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4"
                 HorizontalContentAlignment="Stretch">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0,0,10,0" />
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>

</UserControl>
