<UserControl x:Class="Secretary.View.HomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:scheduler="clr-namespace:Syncfusion.UI.Xaml.Scheduler;assembly=Syncfusion.SfScheduler.WPF"
             xmlns:local="clr-namespace:Secretary.View"
             mc:Ignorable="d" 
             d:DesignHeight="667" d:DesignWidth="804" Background="LightBlue">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="1" Content="Kalendar zakazanih događaja:" Margin="25 0 0 0" FontSize="20" FontWeight="SemiBold"></Label>

        <scheduler:SfScheduler x:Name="homeScheduler" Grid.Row="2" AppointmentEditFlag="None" AllowedViewTypes="Week" ViewType="Week" Margin="30 0 30 30" ShowDatePickerButton="True" BorderBrush="Black" BorderThickness="2">
            <scheduler:SfScheduler.DaysViewSettings>
                <scheduler:DaysViewSettings StartHour="7" EndHour="15" TimeIntervalHeight="60" TimeRulerSize="60" TimeRulerFormat="HH:mm"/>
            </scheduler:SfScheduler.DaysViewSettings>
        </scheduler:SfScheduler>
    </Grid>
</UserControl>
