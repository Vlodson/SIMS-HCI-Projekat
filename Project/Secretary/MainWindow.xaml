<Window x:Class="Secretary.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Secretary"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Background="LightBlue">
        
        <Grid.Resources>
            <Style TargetType="{x:Type Button}"> 
                <Setter Property="Margin" Value="30,0,0,0"/>
            </Style>
        </Grid.Resources>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Button BorderBrush="Black" BorderThickness="1" x:Name="CRUDAcc" Grid.Column="1" Grid.Row="1" Content="CRUD account options" Margin="0,0,10,0" Click="CRUDAcc_Click"/>
        <Button BorderBrush="Black" BorderThickness="1" x:Name="CRUDMedRecords" Grid.Column="2" Grid.Row="1" Content="CRUD medical records" Click="CRUDMedRecords_Click" />
        
    </Grid>
</Window>
